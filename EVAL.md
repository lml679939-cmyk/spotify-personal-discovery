# 演算法驗收紀錄

> 規則：**演算法改動前後各跑一輪，數字寫在這裡**，commit message 引用對應輪次。
> 機器指標：`python eval_bench.py`（訪客 S1–S5，情境輸入逐字固定在腳本裡）。
> 人工三題：① 認得幾首（0–15）② 契合情境（1–5）③ 想點開幾首——每情境約一分鐘。
> S6（登入模式）手動跑：app 內輸入「想聽完全沒聽過的」＋新藝人 100%＋15 首，
> 出圈指標抄結果頁 caption（幾首出圈／平均知名度／擋掉幾首），認得幾首當場數。
>
> 判定（指引，不是硬性 CI）：可播率 < 13/15 要查原因；70% 模式認得 > 3 首＝出圈退步；
> 任一指標比上一輪差 15% 以上，該演算法改動不上線，除非把理由寫在該輪備註。
> LLM 有隨機性：單輪差幾個百分點是雜訊，看趨勢與大幅退步。

## 第 0 輪（追溯基準）：2026-08-20 線上實測

- 模式：登入、新藝人 100%、12 首有效樣本；來源：CLAUDE.md「出圈演算法」段
- 可播：12/15（3 首幻覺被搜尋端擋下，僅附 YouTube 連結；當時尚無同歌手補救）
- 出圈：新藝人 100%；**使用者認得 1/12**（Motion Sickness — Phoebe Bridgers）
- 已知問題：幻覺率 3/15＝20%（歌手真實、歌名配錯）

## 第 1 輪：2026-08-22（機器指標已跑；人工三題＋S6 待補）

> `eval_bench.py --tag round1`，明細 `eval_runs/20260822-0157_round1.json`。
> 五情境全數 15/15 可播、死連結卡 0（第 0 輪是 12/15），overgen＋補救把 11 首搜尋失敗全數吸收。

**怎麼跑**（約一小時，含人工評分）
```powershell
python eval_bench.py --tag round1          # 訪客 S1–S5，需本機 .env 金鑰
```
腳本會逐情境印出 15 首清單與機器指標，並把明細存進 `eval_runs/`。
接著照上方說明在 app 內手動跑 S6，最後把數字填進下表、備註寫下觀察。

**這一輪要特別確認的事**（冒煙測時就看到的疑點）
1. **訪客的 fame 分佈**：冒煙測 S1／S3 時，LLM 幾乎不給 fame ≤ 2（`fame_low_share=0.0`），
   代表訪客版的 fame 天花板（`GUEST_POP_CEILING=80`，只擋 fame 5）可能形同虛設。
   若第 1 輪 S1–S5 的 `fame_low_share` 普遍是 0，就要回頭調 guest prompt 的 fame 錨點
   （登入版當初也踩過同一個坑：只給抽象定義時 LLM 從不給 1-2 分，加了可操作錨點才改善）。
2. **幻覺補救有沒有在動**：看 `repaired` 與 `search_miss`。兩者都是 0 表示這批沒有幻覺
   （很好），但 `search_miss > 0` 而 `repaired == 0` 就要查補救為什麼沒接上。
3. **S4 的 `fav_share`**：prompt 承諾「至少一半來自指定歌手」，實際值低於 0.5 就是沒遵守。

填完這一輪之後，`eval_bench.py --no-repair` 可以再跑一次當對照組，
量出「幻覺補救」對可播率的實際貢獻。

| 情境 | 可播 | 搜尋失敗 | 補救 | 死連結卡 | pop_blocked | 驚喜佔比(fame≤2) | 約束遵守 | 認得 | 契合(1-5) | 想聽 |
|---|---|---|---|---|---|---|---|---|---|---|
| S1 深夜華語 | 15 | 5 | 2 | 0 | 0 | 0.0 | lang=1.0 | 6 | 3 | 5 |
| S2 雨天 Jazz | 15 | 3 | 3 | 0 | 3 | 0.2 | — | 0 | n/a | 5 |
| S3 健身 | 15 | 0 | 0 | 0 | 3 | 0.0 | — | 0 | n/a | 4 |
| S4 指定歌手 | 15 | 1 | 1 | 0 | 3 | 0.07 | fav=0.27 | 4 | 3 | 4 |
| S5 投射推論 | 15 | 2 | 1 | 0 | 2 | 0.07 | — | 3 | 2 | 4 |
| S6 登入探索（手動） | 15 | 4 | 4 | 0 | 10 | —(avg pop 51.7) | 出圈率=100%(15/15) | 0 | 3 | 5 |

（認得／契合／想聽三欄待使用者聽過清單後補；S6 需 app 內 OAuth，CLI 跑不了。）

備註：
- **可播率**：五情境全 15/15、死連結卡 0（第 0 輪 12/15）。搜尋失敗共 11 首，補救成功 7 首、
  其餘由 overgen 餘裕吸收——`dead_cards` 全 0，清單開頭不再出現只有搜尋鈕的死卡。
- **疑點 1（訪客 fame 天花板≈形同虛設，確認）**：`fame_low_share` S1/S3=0、S4/S5=0.07、S2=0.2，
  但 S2 的三首 fame2 與 S5 的 fame2 **全是補救補進來的深軌**（🛠）——LLM 自己幾乎不提 fame≤2。
  尤其 S1 有五首 fame5 國民金曲（安靜／修煉愛情／忽然之間…）全數入列、`pop_blocked=0`：
  華語情境湊不滿時天花板不縮量→大熱門全回補。→ **待辦**：調 guest prompt 的 fame 錨點，
  讓模型自己就產出更多 fame≤2 候選（登入版當初靠「可操作錨點」才改善，同一招）。
- **疑點 2（補救有沒有在動：有，且有效）**：`repaired>0` 於 4/5 情境，無「search_miss>0 而 repaired=0」的漏接。
- **疑點 3（指定歌手未達承諾）**：S4 `fav_share=0.27` < 0.5——實際僅 4/15 來自陳綺貞／落日飛車。
  ⚠️ 量測 caveat：`_loose_match` 跨 CJK↔羅馬拼音（陳綺貞 vs Cheer Chen）比不出，數字可能略低估，
  但仍遠低於門檻。→ **待辦**：guest prompt 對「指定歌手」加權，或程式端保底佔比。
- **資料瑕疵**：S4 #12「非音楽著作物」是 Spotify metadata 雜訊藝名（宮崎駿組曲），且與散步情境不符。
- **工具修正**：`eval_bench.py` 首跑因 Windows cp950 主控台無法編碼 🛠/✗ 而 `UnicodeEncodeError` 整批中斷
  （JSON 都沒存）；已加 stdout/stderr UTF-8 reconfigure。這是這台機器每次跑驗收都會踩的坑，已一勞永逸修掉。

**人工三題（訪客本人評，2026-08-22）**
- 記分約定：認得／想點開＝計數；契合(1-5)＝`round(1 + 4 × 契合首數/15)`。S2/S3 全未試聽、無法憑歌名判契合 → 記 `n/a`。
- S1 認得 6／契合 3（8 首）／想聽 5——想點開集中在大熱門情歌（安靜、忽然之間、魚仔），訪客「熟悉安撫」需求是真實的。
- S2 認得 0／契合 n/a／想聽 5——jazz 完全出圈、連歌手都陌生；**高出圈情境無法憑歌名自評契合**（方法論限制）。
- S3 認得 0（**歌手皆識、單曲不確定**）／契合 n/a／想聽 4——健身神曲 canonical（Till I Collapse／Stronger／Numb-Encore）。
- S4 認得 4／契合 3（7 首）／想聽 4——⭐**想點開 4 首全是指定歌手**（陳綺貞×2、落日飛車×2），但 fav_share 僅 0.27＝指定歌手供給不足直接壓低點閱意願。
- S5 認得 3／契合 2（5 首）／想聽 4——indie「疲憊/例行公事」氛圍尚可；想點開偏經典（Lovely Day／Pink Moon／Electric Feel）。
- 方法論待改：契合(1-5) 對高出圈情境（S2/S3）無效——未來這題建議「試聽後再評」，或改問「看清單想不想聽」。

**第 2 輪方向（依這輪體感排序）**
1. **指定歌手保底佔比**——S4 證據最硬：想點開 100% 落在指定歌手、實際供給僅 27%。guest/login prompt 加權或程式端保底。
2. **訪客「熟悉/均衡/探索」fame 選項 ＋ guest prompt fame 錨點修正**——疑點 1：訪客 LLM 幾乎不自產 fame≤2，故「探索」選項與 prompt 錨點**必須綁一起做**，否則探索模式湊不滿→天花板不縮量→大熱門回補→空轉。

## 第 2 輪（已做第 1 項：指定歌手保底）：2026-08-23

> 只影響 S4（唯一帶 `fav_artists` 的情境），故只跑 S4 前後對照，省共用配額。
> 前：`eval_runs/20260823-0154_before-favfloor.json`；後：`20260823-0215_after-favfloor-final.json`。

**改了什麼**：指定歌手保底走**程式端**（`recommend._apply_fav_floor`），不再只靠 prompt。
LLM 給不夠時，用 `spotify_api.fav_artist_pool()` 直接從點名歌手的 Spotify 專輯目錄拉
**真實深軌**補到 `FAV_MIN_SHARE=0.5`（＝prompt 承諾的「至少一半」），並對點名歌手放寬
同藝人上限、各歌手之間平均分配。**零幻覺**（曲目來自真實目錄，不是 LLM 生的）。

| 指標 | 前 | 後 | 說明 |
|---|---|---|---|
| 指定歌手佔比（人工數清單） | **3/15** | **8/15** | Cheer Chen×1＋落日×2 → Cheer×4＋落日×4，達到保底線 |
| `fav_share`（機器量） | 0.20 | **0.53** | 量法已補 `_fav_pick` 標籤，否則會少算補進來的 Cheer（羅馬拼音對不上）|
| `fav_have` / `fav_added` | —/— | 2 / 4 | LLM 只給 2 首可播（被辨識的落日）→ 程式端補 4 首真實深軌 |
| 可播 | 15/15 | 15/15 | 死連結卡 0；補進來的都是真實深軌，順帶把可播數撐滿 |

**關鍵發現＋踩到的坑**
- ⚠️ **CJK 藝名解析**：`陳綺貞` 在 Spotify 存成 "Cheer Chen"，`_artist_catalog` 的嚴格
  名稱比對跨文字系統對不上 → 第一版只補到落日飛車、Cheer 一首都沒補（fav_share 卡在 0.4）。
  修法：`fav_artist_pool` 走 `_artist_catalog(..., allow_top_result=True)`——使用者親手打的
  藝名，搜尋第一筆幾乎必然是本尊。**只給保底用，幻覺補救維持嚴格**（LLM 給的名不可信）。
  這是 zh-TW app 的**常見**情況，不是邊緣案例。
- **已知限制（可接受，未修）**：被搜尋端解析成羅馬拼音的 LLM 指定歌手卡（如 Cheer Chen）
  沒有 `_fav_artist` 標籤、`_track_matches_fav` 靠名字比不出 → 演算法的 `fav_have` 會**低估**，
  可能多抓一次 pool、偶爾把指定歌手佔比推過 50%。但「保底」是下限不是上限，超標＝更多
  使用者點名的歌手，無害；且 pool 跨使用者快取，多抓成本低。要根治得把 pool 解析到的
  Spotify artist id 傳進 curate 用 id 比對——留待需要時再做。
- 對照第 1 輪：其餘四情境（S1/S2/S3/S5）不帶 fav_artists，`_apply_fav_floor` 直接原樣返回，
  零行為變動（有單元測試釘住），故不重跑。

**人工三題（S4，2026-08-23）**：待補——重點看「想點開幾首」是否從 4 上升
（第 1 輪想點開的 4 首全是指定歌手，但供給只有 3；現在供給 8，理論上想點開數會跟上）。
