[theme]
primaryColor = "#FF69B4"
backgroundColor = "#FFFDF7"
secondaryBackgroundColor = "#FFF0F5"
textColor = "#2D1B4E"
font = "sans serif"

[client]
toolbarMode = "minimal"

[server]
# 程式碼本來就擋 10 MB；不設這個的話 Streamlit 會顯示預設的「200MB per file」，與實際不符
maxUploadSize = 10
