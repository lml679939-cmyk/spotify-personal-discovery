# ── 本地開發用設定 ──
# 雲端部署時，以下同樣的 key 要填在
# Streamlit Cloud → App → Settings → Secrets（TOML 格式，一行一個 KEY = "value"）

# ⚠️ 必填：本站自備的 Gemini Key，所有使用者（含訪客模式）共用這一把
# 從 https://aistudio.google.com/app/apikey 取得
GEMINI_API_KEY=your_gemini_key_here

# ── Spotify（選填，但沒填就無法登入、也無法搜尋歌曲連結）──
# 從 https://developer.spotify.com/dashboard 建立 App 後取得
# 提供兩件事：(1) 訪客模式搜尋歌曲（Client Credentials，不限人數）
#            (2) 使用者登入讀聆聽紀錄（受 Spotify 授權名單人數限制）
SPOTIFY_CLIENT_ID=your_client_id_here
SPOTIFY_CLIENT_SECRET=your_client_secret_here

# 本地跑 Streamlit 預設 port 8501；雲端部署改成 https://你的app.streamlit.app/
# ⚠️ 這個值必須跟 Spotify Developer Dashboard 的 Redirect URIs 一字不差
SPOTIFY_REDIRECT_URI=http://127.0.0.1:8501/
